// ternary operator

// let age = 5;
// let drink;

// if (age >= 5) {
//   drink = "coffee";
// } else {
//   drink = "milk";
// }

// console.log(drink);

// ternary operator / conditional operator

// a conditional operator that assigns a value to a variable based on some condition

let age = 18;
// let drink = age >= 18 ? "coffee" : "milk";
// console.log(drink);

let bro = age >= 14 ? "Big b" : "younger bro";
console.log(bro);
