// array push pop
// push == add in last [adds a new element to an array (at the end)]
// pop == remove from last [removes the last element from an array]

// array shift unshift
// shift == remove first element [removes the first array element and "shifts" all other elements to a lower index.]
// unshift == add in first[adds a new element to an array (at the beginning), and "unshifts" older elements]

let fruits = ["apple", "mango", "grapes"];
console.log(fruits);

// push  ==  adds a new element to an array (at the end)
// fruits.push("banana");
// console.log(fruits);

// pop  == removes the last element from an array
// let poppedFruit = fruits.pop();
// console.log(fruits);
// console.log("popped fruits is", poppedFruit);

// unshift  == adds a new element to an array (at the beginning), and "unshifts" older elements
// fruits.unshift("banana");
// fruits.unshift("myfruit");
// console.log(fruits);

// shift  ==  removes the first array element and "shifts" all other elements to a lower index.
// let removedFruit = fruits.shift();
// console.log(fruits);
// console.log("removed fruits is ", removedFruit);
