// and  or operator

// The &&(AND) Operator (Logical AND)

let firstName = "rahul";
let age = 26;

// if (firstName[0] === "r") {
//   console.log("your name starts with r");
// }

// if (age > 18) {
//   console.log("you are above 18");
// } else {
//   console.log("you are below 18");
// }

// The && operator returns true if both expressions are true, otherwise it returns false.

if (firstName[0] === "r" && age > 18) {
  console.log("Name starts with r and above 18  AND(&&)");
} else {
  console.log("inside else AND(&&)");
}

// The || Operator (Logical OR)
// The || returns true if one or both expressions are true, otherwise it returns false.

// let firstName = "rahul";
// let age = 16;

// if (firstName[0] === "r" || age > 18) {
//   console.log("inside if OR(||)");
// } else {
//   console.log("inside else OR(||)");
// }
